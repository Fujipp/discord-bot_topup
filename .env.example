# ==========================================
# Discord Bot Configuration
# ==========================================
# Bot Token จาก Discord Developer Portal
DISCORD_TOKEN=your_bot_token_here

# Application ID (Client ID) จาก Discord Developer Portal
DISCORD_CLIENT_ID=your_app_id_here

# Guild ID สำหรับ deploy commands แบบ guild-specific (optional)
# ถ้าไม่ใส่จะ deploy แบบ global
# DISCORD_GUILD_ID=your_dev_guild_id

# ==========================================
# Server Configuration (PaaS Deployment)
# ==========================================
# Port สำหรับ HTTP server (สำหรับ health check)
# Host จะกำหนดให้อัตโนมัติ หรือใช้ default 8080
PORT=8080

# ==========================================
# SlipOK API Configuration (ตรวจสอบสลิปธนาคาร)
# ==========================================
# API Key จาก SlipOK - รับได้จาก https://slipok.com
API_SLIPOK_KEY=your_slipok_api_key

# Branch ID จาก SlipOK (รหัสท้ายลิงก์ไม่ใช่ลิงก์เต็ม)
SLIPOK_BRANCH_ID=your_branch_id

# ==========================================
# TrueMoney Wallet API Configuration
# ==========================================
# API Key สำหรับ TrueMoney Voucher Service
API_TRUEMONEY_KEY_ID=your_truemoney_api_key

# Base URL ของ TrueMoney API Service (optional - มี default)
TRUEMONEY_BASE=https://true-wallet-voucher-production.up.railway.app

# เบอร์โทรศัพท์รับเงิน TrueMoney (10 หลัก)
TRUEMONEY_PHONE=0xxxxxxxxx

# ==========================================
# Config UI System
# ==========================================
# ใช้คำสั่ง /setup ใน Discord เพื่อจัดการการตั้งค่าแบบ GUI
# ระบบจะเก็บค่าที่ตั้งไว้ใน update/logdata.json
# Environment variables ที่ตั้งไว้จะมี priority สูงกว่า config file
# 
# ระดับ Priority ของ Config:
# 1. Environment Variables (.env) - สูงสุด
# 2. Config File (update/logdata.json) - ต่ำกว่า
# 3. Default Values - ต่ำสุด
